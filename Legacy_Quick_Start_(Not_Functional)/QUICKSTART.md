# Legacy Quick Start Instructions (Not Functional)

These instructions were used for development only. They do not work in the submitted version.

---

## Quick Start (Legacy)

From the project root, run:

```
./start.ps1
```

To stop the app:

```
./stop.ps1
```

- This would launch both backend and frontend automatically in development.
- Not supported in the submission package. Use the Windows installer instead.
